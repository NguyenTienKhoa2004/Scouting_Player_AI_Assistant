# Backend application

Reserved for the FastAPI HTTP application. Business logic must stay in
`packages/matchmind`; this app owns transport concerns such as routes, schemas,
authentication, and dependency wiring.
