[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "matchmind"
version = "0.1.0"
description = "Reusable data, football analytics, and ML logic for MatchMind."
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]
